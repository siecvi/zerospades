//This header is generated by CMake, do not edit it. (Changes will be lost)
// Instead edit ZeroSpades.h.in, and run CMake again.

// OpenSpades client identity bytes sent in the AoS server handshake.
// Frozen at 0.1.5 — see CMakeLists.txt.
#define OPENSPADES_VERSION_MAJOR @OPENSPADES_VERSION_MAJOR@
#define OPENSPADES_VERSION_MINOR @OPENSPADES_VERSION_MINOR@
#define OPENSPADES_VERSION_PATCH @OPENSPADES_VERSION_PATCH@

// ZeroSpades application version (single source of truth: CMakeLists.txt).
#define ZEROSPADES_VERSION_MAJOR @ZEROSPADES_VERSION_MAJOR@
#define ZEROSPADES_VERSION_MINOR @ZEROSPADES_VERSION_MINOR@
#define ZEROSPADES_VERSION_PATCH @ZEROSPADES_VERSION_PATCH@
#define ZEROSPADES_FULL_VERSION  "@ZEROSPADES_FULL_VERSION@"

#ifdef GIT_COMMIT_HASH
	#define ZEROSPADES_VER_STR "ZeroSpades " ZEROSPADES_FULL_VERSION " " GIT_COMMIT_HASH
#else
	#define ZEROSPADES_VER_STR "ZeroSpades " ZEROSPADES_FULL_VERSION
#endif

#include "Core/VersionInfo.h"

#define ZEROSPADES_EXTRATITLE	"${ZEROSPADES_EXTRATITLE}"
#define PACKAGE_STRING			ZEROSPADES_VER_STR ZEROSPADES_EXTRATITLE

#define ZEROSPADES_COMPILER_STR	"${ZEROSPADES_COMPILER}"

#define SDL_VERSION_STR			"${SDL_VERSION_STRING}"
#define ZLIB_VERSION_STR		"${ZLIB_VERSION_STRING}"
#define CURL_VERSION_STR		"${CURL_VERSION_STRING}"

#cmakedefine RESDIR_DEFINED
#define RESDIR "${RESDIR}"

#define CMAKE_INSTALL_PREFIX "${CMAKE_INSTALL_PREFIX}"
#define ZEROSPADES_INSTALL_RESOURCES "${ZEROSPADES_INSTALL_RESOURCES}"
